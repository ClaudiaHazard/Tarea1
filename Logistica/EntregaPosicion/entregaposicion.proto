syntax = "proto3";
package entregaposicion;

option go_package = "github.com/example/path/gen;entregaposicion";

message Message {
    string body = 1;
}

service EntregaPosicionService{
    rpc EntregaPosicion(Message) returns (Message) {}
}