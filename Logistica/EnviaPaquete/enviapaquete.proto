syntax = "proto3";
package enviapaquete;

option go_package = "github.com/example/path/gen;enviapaquete";

message Message {
    string body = 1;
}

service ConexionService{
    rpc SayHello(Message) returns (Message) {}
}