syntax = "proto3";
package main;

option go_package = "github.com/example/path/gen;sendpaquete";

message Message {
    string body = 1;
}

service ConexionService{
    rpc SayHello(Message) returns (Message) {}
}